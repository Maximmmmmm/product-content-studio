{
  "name": "product-content-studio",
  "version": "0.1.0",
  "private": true,
  "description": "Product Content Studio — Junior Full Stack technical assignment (backend: NestJS, frontend: Next.js).",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev:backend": "npm run start:dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build:backend": "npm run build --prefix backend",
    "build:frontend": "npm run build --prefix frontend",
    "build": "npm run build:backend && npm run build:frontend",
    "lint:backend": "npm run lint --prefix backend",
    "lint:frontend": "npm run lint --prefix frontend",
    "lint": "npm run lint:backend && npm run lint:frontend",
    "test:backend": "npm test --prefix backend",
    "test:backend:e2e": "npm run test:e2e --prefix backend",
    "test": "npm run test:backend",
    "db:migrate": "npm run prisma:migrate --prefix backend",
    "db:seed": "npm run prisma:seed --prefix backend"
  }
}
